import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'capstone.mcr.abuyog',
    appName: 'MCR Records',
    webDir: 'www',
    server: {
        androidScheme: 'https',
    },
};

export default config;
